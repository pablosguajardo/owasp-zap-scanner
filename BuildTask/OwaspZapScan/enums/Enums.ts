export enum ReportType {
    XML,
    HTML,
    MD,
    ALL,
    HTMLORIG
}

export enum AlertRowType {
    InstanceRow, 
    AlertRow
}

export enum ZapScanType {
    Spider,
    Active,
    AjaxSpider,
    OpenApi
}